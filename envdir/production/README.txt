The following files are required in the production server's envdir for this
app, with the contents being the value of that setting.

For example, consider the resulting file: `$ echo 'False' > DEBUG`

DB_PASSWORD
ENVDIR_LOCATION
SECRET_KEY
STATIC_ROOT
